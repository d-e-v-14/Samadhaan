function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="badge">JanSeva</p>
        <h1>Admin Dashboard Setup Complete</h1>
        <p>
          The frontend workspace is now ready. Next, we can build live complaint
          queues, triage tools, and SLA tracking.
        </p>
      </section>
    </main>
  );
}

export default App;
